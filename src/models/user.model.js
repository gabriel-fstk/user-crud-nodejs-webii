class User {

  constructor(id, cpf, name, password, profile) {
    this.id = id;
    this.cpf = cpf;
    this.name = name;
    this.password = password;
    this.profile = profile;
  }
}

module.exports = User;
